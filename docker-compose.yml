version: "3"
services:

  picbox:
    build: .
    restart: always
    volumes:
      - ./pycbox.py:/pycbox/pycbox.py
      - ./files:/pycbox/files
    ports:
      - "5000:5000"
    environment:
      - PYCBOX_CONFIG=config.yml
    cap_drop:
      - all
